import {useEffect,useState} from 'react';import {games} from '../data/games';import {GameGrid} from '../components/GameCard';import {SectionHeading} from '../components/UI';import {useStorage} from '../hooks/useStorage';
function Countdown(){const[end]=useStorage('gv-promo-end',Date.now()+3*86400000);const[now,setNow]=useState(Date.now());useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);const seconds=Math.max(0,Math.floor((end-now)/1000));return <div><small>{seconds?'DEMO EVENT ENDS IN':'DEMO EVENT HAS ENDED'}</small><div className="countdown">{[[Math.floor(seconds/86400),'DAYS'],[Math.floor(seconds/3600)%24,'HRS'],[Math.floor(seconds/60)%60,'MIN'],[seconds%60,'SEC']].map(([n,label])=><div key={label}><strong>{String(n).padStart(2,'0')}</strong><span>{label}</span></div>)}</div></div>}
export default function Deals(){return <div className="container page"><section className="deals-header"><div><div className="eyebrow">THE VAULT SALE</div><h1>Your wishlist.<br/>At a better price.</h1><p className="mt-4">Save up to 60% on your next great adventure.</p></div><Countdown/></section><SectionHeading title="Small prices. Big possibilities." link="Browse the full store"/><GameGrid games={games.filter(g=>g.discount>0).sort((a,b)=>b.discount-a.discount)}/><p className="notice">Demo promotions and fictional prices. The countdown is for this device’s demo event.</p></div>}
