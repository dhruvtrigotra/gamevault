import {Link} from 'react-router-dom';import {ArrowUpRight} from 'lucide-react';import {categories,games} from '../data/games';
export default function Categories(){return <div className="container page"><div className="eyebrow">CHOOSE YOUR ADVENTURE</div><h1 className="page-title">A world for every player</h1><p className="page-subtitle">Find your favorites. Discover something different.</p><div className="category-grid">{categories.map(c=>{const matches=games.filter(g=>g.genre.includes(c));return <Link key={c} to={`/store?genre=${c}`} className="category-tile" style={{backgroundImage:`linear-gradient(0deg,#110a1df0,#110a1d22),url('${matches[0].coverImage}')`}}><h2>{c}</h2><span>{matches.length} games to discover <ArrowUpRight size={20}/></span></Link>})}</div></div>}
